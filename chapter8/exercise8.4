While running ./a.out multiple times, when first ./a.out after completing the execution it gets terminated and simultaneously its child and another ./a.out starts their execution and got stucked in race conditions and this problem can be solved by using critical section.
